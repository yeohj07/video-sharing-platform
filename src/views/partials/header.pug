header
    a(href="/").header__logo
        i.fab.fa-youtube
        |  <span style="color:white;">GSA Shorts<span>
    nav
        ul 
            li 
                a(href="/search")
                    i.fas.fa-search
            if loggedIn
                li  
                    a(href="/videos/upload") 업로드
                li 
                    a(href="/users/edit") 프로필 수정
                li 
                    a(href="/users/logout") 로그아웃
                li 
                    a(href=`/users/${loggedInUser._id}`)
                        if !loggedInUser.avatarUrl
                            i.fas.fa-user.fa-lg
                        else 
                            div.header__avatar(style=`background-image:url(${loggedInUser.avatarUrl});background-size:cover;background-position:center;`)

            else
                li 
                    a(href="/login") 로그인
                li 
                    a(href="/join").header__btn 회원가입
                
           
